name: Development

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize
      - reopened

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore ElevatedTutors/ElevatedTutors.csproj

      - name: Build project
        run: dotnet build ElevatedTutors/ElevatedTutors.csproj --configuration Release --no-restore

      - name: Run tests
        run: dotnet test ElevatedTutors/ElevatedTutors.csproj --no-build --verbosity normal
