name: 🚀 Build and Test ASP.NET App

on:
  push:
    branches:
      - main
      - alpha
      - beta

permissions:
  contents: write
  actions: read

jobs:
  build:
    name: 🏗️ Build, Test, and Publish
    runs-on: ubuntu-latest
    steps:
      - name: ☁️ Checkout repository
        uses: actions/checkout@v4

      - name: 🔧 Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: 📦 Restore dependencies
        run: dotnet restore ElevatedTutorsWebsite/ElevatedTutorsWebsite.csproj

      - name: 🧪 Build project
        run: dotnet build ElevatedTutorsWebsite/ElevatedTutorsWebsite.csproj --configuration Release --no-restore

      - name: 🧪 Run tests
        run: dotnet test ElevatedTutorsWebsite/ElevatedTutorsWebsite.csproj --no-build --verbosity normal

      - name: 📦 Publish app
        run: dotnet publish ElevatedTutorsWebsite/ElevatedTutorsWebsite.csproj -c Release -o ./publish

      - name: 📂 Upload published app
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-publish
          path: publish

# Uncomment and configure the deploy job once Azure App Service is ready
#  deploy:
#    name: 🚀 Deploy to Azure App Service
#    runs-on: ubuntu-latest
#    needs: build
#    steps:
#      - name: ☁️ Checkout repository
#        uses: actions/checkout@v4
#
#      - name: 📂 Download published app
#        uses: actions/download-artifact@v4
#        with:
#          name: dotnet-publish
#          path: ./publish
#
#      - name: 🔑 Azure WebApp Deploy
#        uses: azure/webapps-deploy@v3
#        with:
#          app-name: YOUR_APP_SERVICE_NAME
#          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
#          package: ./publish
